// Este módulo está deprecado. Los componentes son standalone.
// Las rutas cargan los componentes dinámicamente.

import { NgModule } from '@angular/core';

@NgModule({})
export class CreditsModule {}
